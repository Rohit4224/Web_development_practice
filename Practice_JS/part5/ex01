let arr = [2, 5, 6, 45, 42, 56, 85, 0, 7, 65];
let n = 40;

function larger_ele (arr, n)
{
    for (let i = 0; i < arr.length; i++)
    {
        if (arr[i] > n)
            console.log(arr[i]);
    }
}

larger_ele(arr, n);